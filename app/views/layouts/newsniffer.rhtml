<!DOCTYPE html>
<html>
<head>
  <meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
	<title><%= h(@title) + " - " if @title -%>News Sniffer</title>
	<link href="/stylesheets/newsniffer.css" media="screen" rel="Stylesheet" type="text/css" />
	<%= javascript_include_tag :defaults, :cache => true %>
  <%= yield :head %>
</head>
<body>
	<div id="header">
		<img src="/stylesheets/layout/newsniffer-text.png" alt="News Sniffer" id="logo"/>
		<div id="nav">
			<ul>
				<li>
				<%= link_to 'News Article Revisions', versions_url, 
          :title => 'News article Revisions',
            :class => controller.controller_name == "versions" ? "selected" : "" %>
				</li>
				<li><a href="/blog/">Blog</a></li>
				<li>
					<%= link_to 'About', about_url, :class => request.url == about_url ? "selected" : "" %>
				</li>
			</ul>
		</div> 
	</div>
	<div id="crossbar"></div>
	<div id="content">
    <% if flash[:notice] %>
			<div id="notice"><%= flash[:notice] %></div>
    <% end -%>
    <% if flash[:error] %>
			<div id="error"><%= flash[:error] %></div>
    <% end -%>

    <%= yield %>
  </div>

	<div id="footer">
	</div>
</body>
</html>
